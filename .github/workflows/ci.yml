name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  UV_FROZEN: "1"

jobs:
  rust-checks:
    name: Rust Checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run Rust tests
        run: cargo test

  test-python:
    name: Test Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14"]

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-py${{ matrix.python-version }}-${{ hashFiles('**/Cargo.lock') }}

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Build wheel for Python ${{ matrix.python-version }}
        run: |
          uv venv --python ${{ matrix.python-version }}
          uv pip install maturin
          uv run maturin build --release --interpreter python${{ matrix.python-version }}

      - name: Install wheel and test dependencies
        run: |
          uv pip install pytest pytest-asyncio hypothesis "moto[dynamodb,server]" boto3 pydantic ruff
          uv pip install target/wheels/*.whl

      - name: Run Python tests
        run: uv run pytest tests/ -v

      - name: Upload wheel
        uses: actions/upload-artifact@v4
        with:
          name: wheel-py${{ matrix.python-version }}
          path: target/wheels/*.whl

  test-minimum-versions:
    name: Test Minimum Versions (Python 3.11)
    needs: test-python
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Set up Python 3.11
        run: uv python install 3.11

      - name: Download wheel
        uses: actions/download-artifact@v4
        with:
          name: wheel-py3.11
          path: dist/

      - name: Install wheel and minimum dependencies
        run: |
          uv venv --python 3.11
          uv pip install "pytest>=8.0,<8.1" "pytest-asyncio>=0.24,<0.25" "hypothesis>=6.100,<6.101" "moto[dynamodb,server]>=5.0,<5.1" "boto3>=1.35,<1.36" "pydantic>=2.0,<2.1"
          uv pip install dist/*.whl

      - name: Run Python tests
        run: uv run pytest tests/ -v
